@model IEnumerable<sistemaInventario.Models.Categoria>
@{
    ViewBag.Title = "Categorías";
}

<h2>Lista de categorías</h2>

<div class="card mb-4">
    <div class="card-header">
       <i class=""></i> Listado de categorías
    </div>
    <div class="card-body">
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Descripción</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>@item.id_categoria</td>
                    <td>@item.nombre</td>
                    <td>@item.descripcion</td>
                    <td>
                        <a href="/Categorias/Edit/@item.id_categoria" class="btn btn-warning">Editar</a>
                        <a href="/Categorias/Delete/@item.id_categoria" class="btn btn-danger">Eliminar</a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>
